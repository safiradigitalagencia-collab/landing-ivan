import re

with open('style.css', 'r', encoding='utf-8') as f:
    css = f.read()

# I will write a script to refactor the css systematically. 
# But first I need to inspect it more or just do text replacements.
